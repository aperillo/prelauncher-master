<div class="hero">
  
  <p class="large brandon">LagLezz HAS LANDED</p>
  <p class="small brandon">Taking gaming to <b>quantam</b> levels.<br>Don't be a noob. Join the <b>Laglezz legion</b>.</p>
  
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>